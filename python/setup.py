#!/usr/bin/env python

from distutils.core import setup

setup(name='loom',
      version='1.0',
      description='Python interface for Loom - workflow system',
      author='Loom team',
      url='',
      packages=['loom'])
